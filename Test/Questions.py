# class Question:
#     def __init__(self, prompt, answer, per_score):
#         self.prompt = prompt
#         self.answer = answer
#         self.per_score = per_score

# def find_K(S):
#     N = len(S)
#     K = 0
#     for i in range(1, N):
#         K += (S[i] - S[0]) / (N - 1)
#     return K / N

# S = [7, 5, 6]
# print(find_K(S))


print(find_k(S))
